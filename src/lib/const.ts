export const COURSE_NAME = ['REG', 'INT', 'HDS', 'RC']

export enum Courses {
    REG = 0,
    INT = 1,
    HDS = 2,
    RC = 3
}

export enum Roles {
    UNAUTHORIZED = "unauthorized",
    USER = "user",
    ADMIN = "admin"
}

export enum InviteStatus {
    UNUSED = "unused",
    CLAIMED = "claimed"
}